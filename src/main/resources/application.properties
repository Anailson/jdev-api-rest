#DEFININDO A CONEX�O COM O BANCO DE DADOS
spring.datasource.url=jdbc:postgresql://localhost:5432/curso-spring-api-angular
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver
#Definindo sempre que houve mapeamento(JPA) seja criado de forma autom�tica na base de dados
spring.jpa.hibernate.ddl-auto=update

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

#contexto da aplicação - http://localhost:8080/cursospringrestapi/
server.servlet.context-path=/cursospringrestapi

#spring.sql.init.platform=postgres

#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

#CONFIGURAÇÃO PARA O POOL DE CONEXÃO

#numero máximo de milissegundos de um cliente aguardar por uma conexão
spring.datasource.hikari.connection-timeout= 20000

#numero minimo de conexões inativas mantidas pelo KIkariCP em um conjunto de conexões
spring.datasource.hikari.minimum-idle=10

#maximo do pool de conexão
spring.datasource.hikari.maximum-pool-size=40

#tempo ocioso para conexão
spring.datasource.hikari.idle-timeout=10000

#salvando dados no banco automaticamente
spring.datasource.hikari.auto-commit=true


